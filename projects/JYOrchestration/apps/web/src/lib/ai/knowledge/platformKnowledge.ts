/**
 * 플랫폼 기본 지식(하드코딩) — 벡터 DB·사용자 KB 없이 AI 실행 시 system에 주입.
 */

export const PLATFORM_KNOWLEDGE = {
  feature_designer: `
기능정리 기준:
- 사용자 흐름(단계·목표·다음 행동)을 기준으로 기능을 정의한다.
- 각 기능에 예외·실패·빈 입력·권한 거부 등 예외 처리를 반드시 포함한다.
- 화면·상태값(로딩/성공/실패/빈 목록 등)을 명시하고, 상태 전이가 모호하지 않게 쓴다.
- UI와의 연결(어떤 화면/컴포넌트·입력·피드백)을 고려해 서술한다.
- 서비스 흐름 기반으로 기능을 묶고, 각 단계별 핵심 기능을 도출한다.
- 사용자 입력/출력(필드·알림·다음 단계)을 명확히 정의한다.
- 최소 4~8개 기능 도출을 목표로 하되, 구현 가능성을 유지한다.
- 질문할 때는 반드시 추천안과 선택지를 함께 제시한다.
`.trim(),

  designer: `
UX/UI 기준:
- 모바일 우선: 작은 화면·엄지 도달 영역을 기본으로 설계한다.
- 버튼 위치·간격: 주요 CTA는 하단·엄지 영역에 두고, 터치 타깃 최소 44px, 요소 간 간격을 넉넉히 둔다.
- 스크롤 최소화: 한 화면에 한 목적; 긴 폼은 단계 분할·접이식 섹션으로 줄인다.
- 입력 최소화: 필수 입력만 남기고, 기본값·자동완성·선택형을 우선한다.
- 접근성: 대비·포커스 순서·스크린리더 레이블·오류 메시지를 명확히 한다.
- 한 화면 한 목적, 가독성 중심 레이아웃을 유지한다.
`.trim(),

  security: `
보안 점검 기준:
- API Key/Secret 노출: sk- 접두, Bearer 토큰, AWS/Stripe 등 키 패턴, 클라이언트 번들·HTML·JS 내 하드코딩을 찾는다.
- .env·환경설정 파일·config 내 비밀값·샘플 키가 저장소/빌드 산출물에 포함되지 않았는지 확인한다.
- 디버그 코드·콘솔 로그에 민감정보·전체 응답 바디가 남지 않았는지 확인한다.
- 관리자·내부용 경로(/admin, /internal, /debug 등)가 공개 정적 배포에 노출되지 않았는지 확인한다.
- 공개 URL·Pages 프리뷰에 내부 호스트·토큰·개인정보가 섞이지 않았는지 확인한다.
- XSS·위험한 외부 스크립트·개인정보 하드코딩·GitHub Pages 공개 리스크를 점검한다.
`.trim(),
} as const;
