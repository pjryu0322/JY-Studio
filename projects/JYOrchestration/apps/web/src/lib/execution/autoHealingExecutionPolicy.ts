export const AUTO_HEALING_AUTO_RUN_ENABLED = true;

// One source failure execution should only auto-run a limited number of tasks.
export const AUTO_HEALING_MAX_AUTO_RUN_TASKS_PER_FAILURE = 2;

// Each AUTO_HEALING task should only be auto-run once.
export const AUTO_HEALING_MAX_AUTO_RUN_ATTEMPTS_PER_TASK = 1;

