import type { CSSProperties } from "react";

export const requirementsWorkspaceShellStyle: CSSProperties = {
  display: "flex",
  flexDirection: "column",
  gap: 0,
  flex: "1 1 auto",
  minHeight: 0,
  width: "100%",
  overflow: "hidden",
};

export const requirementsWorkspaceMainRowStyle: CSSProperties = {
  flex: "1 1 auto",
  minHeight: 0,
  gap: 0,
  border: "1px solid #e2e8f0",
  borderRadius: 16,
  overflow: "hidden",
  background: "#fff",
  boxShadow: "0 18px 50px -24px rgba(15, 23, 42, 0.18)",
};

export const requirementsIdeationChatPanelShellStyle: CSSProperties = {
  flex: "1 1 0%",
  minWidth: 0,
  display: "flex",
  flexDirection: "column",
  minHeight: 0,
  height: "100%",
  overflow: "hidden",
};
