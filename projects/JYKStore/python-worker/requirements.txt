beautifulsoup4>=4.12.0
lxml>=5.0.0
# Optional: PDF parsing (skip gracefully if not installed)
# docling>=2.0.0

# Local E5 embeddings (mode=local_e5). Required to generate embeddings.json.
# The model itself is NOT installed by pip — it is downloaded once via
# `npm run embedding-model:install` and loaded offline from a local directory
# (JYKSTORE_PYTHON_WORKER_E5_MODEL_PATH). Not needed for deterministic_stub tests.
# sentence-transformers>=3.3.1
# huggingface-hub>=0.27.0
